#     _____      __  
#    / __(_)____/ /_ 
#   / /_/ / ___/ __ \
#  / __/ (__  ) / / /
# /_/ /_/____/_/ /_/ 
                   
# This is the custom fish shell configuration file
# Core config.fish is still in ~/.config/fish/config.fish

# You can add your own customizations here, such as aliases, functions, and environment variables.
# Example:

# Android tools
# set -gx QT_QPA_PLATFORM xcb
# set -gx ANDROID_HOME $HOME/Android/Sdk
# set -gx ANDROID_SDK_ROOT $HOME/Android/Sdk
# fish_add_path $ANDROID_HOME/emulator
# fish_add_path $ANDROID_HOME/platform-tools
# fish_add_path $ANDROID_HOME/cmdline-tools/latest/bin

# Aliases (abbrs)
abbr hello 'echo "Hello, World!"'

# Greeting fastfetch
# Uncomment the following line to enable your greeting
# This will disable the default fish greeting (such as the random greeting message):
# function fish_greeting
#     fastfetch
# end