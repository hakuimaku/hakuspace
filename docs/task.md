### Pha 6: Cấu trúc lại thư mục scripts (Core)
- [x] Đổi tên thư mục `src/home/.local/share/hakuspace` thành `src/home/core`.
- [x] Cập nhật `scripts/variables.sh`:
  - Xoá biến `SOURCE_SHARE` và `DEST_SHARE`.
  - Thêm biến `SOURCE_CORE="$HOME_SRC_DIR/core"`.
- [x] Viết lại hàm `deploy_hakuspace_scripts` trong `scripts/functions.sh`:
  - Không copy vào `~/.local/share/hakuspace` nữa.
  - Sử dụng hàm chung `deploy_config_item "$SOURCE_CORE" "$DEST_BIN"` để đệ quy trực tiếp (symlink hoặc copy tuỳ chế độ) từ repo ra thẳng `~/.local/bin`.
- [x] Cập nhật `doctor.sh`:
  - Xoá phần check script cũ.
  - Chèn logic check mới: `check_symlink_recursive "$SOURCE_CORE" "$DEST_BIN"`.
- [x] Cập nhật `rollback.sh`:
  - Xoá `DEST_SHARE` khỏi danh sách `MANAGED_DESTINATIONS`.
  - Đảm bảo việc làm sạch các symlink trong `~/.local/bin` trỏ về repo trước khi rollback, và tự động xử lý thư mục `~/.local/bin` qua cơ chế khôi phục đệ quy giống cấu hình.
